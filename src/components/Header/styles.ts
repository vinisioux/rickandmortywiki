import styled from "styled-components";

export const Container = styled.div`
  height: 10rem;
  background: var(--gray);
  padding: 0 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
`;
